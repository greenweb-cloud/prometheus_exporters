[
{% for item in rabbitmq_ip %} 
   {% set value = item.split('.') %}
   {
        "targets": [
            "{{ item }}"
        ],
        "labels": {
            "rabbit": "{{ value[0] }}"
        }
    }
{% if not loop.last %}
    ,
{% endif %}
{% endfor %}
]
