[
{% for item in nfs_ip %} 
   {% set value = item.split('.') %}
   {
        "targets": [
            "{{ item }}"
        ],
        "labels": {
            "nfs": "{{ value[0] }}"
        }
    }
{% if not loop.last %}
    ,
{% endif %}
{% endfor %}
]
